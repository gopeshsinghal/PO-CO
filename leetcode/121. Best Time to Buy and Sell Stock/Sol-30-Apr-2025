for(let i = 0; i<arr.length; i++){
    if(arr[i]< minPrice){
        minPrice = arr[i]
    }
    if(arr[i] - minPrice > currentProfit){
    let minPrice = arr[0]
let currentProfit = 0
        currentProfit = arr[i] - minPrice
var maxProfit = function(arr) {
 * @return {number}
 */
 * @param {number[]} prices
/**